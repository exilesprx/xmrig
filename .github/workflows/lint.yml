name: Lint

on:
  pull_request:
    types:
      - opened

jobs:
  lint:
    runs_on: ubuntu-latest
    steps:
      - actions/checkout@v3

      - name: Lint
        run: docker run --rm -i hadolint/hadolint < Dockerfile